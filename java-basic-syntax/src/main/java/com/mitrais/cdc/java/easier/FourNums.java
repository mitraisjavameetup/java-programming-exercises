package com.mitrais.cdc.java.easier;

import java.util.ArrayList;
import java.util.List;

public class FourNums {
  public static List<Double> fourNums() {
	  /* To Do :
	   * Make a new class whose main method creates an array of 4 random doubles (each between 0 and 1). 
	   * Use one-step array allocation: double[] nums = { ... }; 
	   * Loop down the array and return the values. 
	   */
    List<Double> result = new ArrayList<Double>(); 
	return result;
  }
}