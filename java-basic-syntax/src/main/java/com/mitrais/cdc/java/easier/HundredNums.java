package com.mitrais.cdc.java.easier;

/** Two-step array allocation.
 *  <p>
 *  From <a href="http://courses.coreservlets.com/Course-Materials/">the
 *  coreservlets.com tutorials on JSF 2, PrimeFaces, Ajax, JavaScript, jQuery, GWT, Android,
 *  Spring, Hibernate, JPA, RESTful Web Services, Hadoop, Spring MVC,
 *  servlets, JSP, Java 8 lambdas and streams (for those that know Java already), 
 *  and Java 8 programming (for those new to Java)</a>.
 */
public class HundredNums {
  public static double runHundredNums(int manyTimes) {
	  /*
	   * Make a new class whose method creates an array of manyTimes random numbers. 
	   * Use two-step array allocation: 
	   * double[] nums = new double[number];
	   * for (int i=0; i<nums.length; i++) {
	   * 	(TODO)
	   * }
	   * After the array has been created, loop down the array, 
	   * calculate the sum of the square roots of the values, 
	   * and return the result.
	   */
    return 0.0;
  }
}