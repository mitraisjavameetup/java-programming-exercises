package com.mitrais.cdc.java;

public class StringUtils {
  public static int eChecker(String s1, String s2) {
	  // To Do : Create String comparator by wether the string has char "e".
	  /*
	   * The compare method should return a negative number if the first entry is
	   * has e than the second, a positive number if the first entry didn't have e than the second, and 0
	   * if they are the same.
	   */
	  int compareFlag = 0;
	  return (compareFlag);
  }
  
  private StringUtils() {} // Uninstantiatable class
}
