package com.mitrais.cdc.java.enums;

public enum Coin {
    HEADS, TAILS;
	
	public static Coin flip() {

	  //Create calculation to return head or tail with equal probability
	  //later, create test case (main class) to call this method (e.g. looped 10 times) 
	  return Coin.HEADS; //replace this
  }
}
